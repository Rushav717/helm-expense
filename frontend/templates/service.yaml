apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: expense
spec:
  type: LoadBalancer
  selector:
        app: frontend
        tier: frontend
        purpose: expense
  ports:
    - protocol: TCP
      port: 80 #service port
      targetPort: 80