apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: expense
spec:
  selector:
        app: backend
        tier: backend
        purpose: expense
  ports:
    - protocol: TCP
      port: 8080 #service port
      targetPort: 8080 